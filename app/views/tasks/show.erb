<h1> <%= @task.title %> </h1>

<% if @task.no_subtask == 1 %>
   <h3> This task has no subtasks </h3>
   <h4> Estimated Duration: </h4><%= @task.estimated_duration %>
   <h4> Deadline: </h4> <%= @task.priority %>
   <h4> Notes: </h4> <%= @task.notes %>


<% else %>
  <h3>Add or Delete Subtasks here: </h3>

<% end %>




<!-- #This Shows A Single Task


User Filters At Top:

Task Title, and general info:

    subtask link
    subtask link
    subtask link


(obviously if there is only a single task/subtask, no subtask link needed) -->
